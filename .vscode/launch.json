{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows configuration",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/lemao-bin.exe",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "build debug windows"
        }
    ]
}